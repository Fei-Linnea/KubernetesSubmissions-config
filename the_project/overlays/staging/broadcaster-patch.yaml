apiVersion: apps/v1
kind: Deployment
metadata:
  name: broadcaster
  namespace: project
spec:
  selector:
    matchLabels:
      app: broadcaster
  template:
    spec:
      containers:
        - name: broadcaster
          env:
            - $patch: delete
              name: DISCORD_WEBHOOK_URL
